# PROMPTS.md — AI 도구 활용 기록

> 작성자: 송상준 · 도구: Claude (Cowork) / Claude Code
> 본 문서는 프로토타입 개발 과정에서 LLM 기반 코딩 도구에 사용한 주요 프롬프트와 그에 따른 의사결정을 기록한다. 목적은 개발 과정의 추적성과 재현성 확보이며, 전체 대화가 아닌 결정적 프롬프트를 발췌한다. 2단계(구현·검증) 기록은 하단에 추가한다.

## 작업 원칙

- **명세 우선(specification-first)**: 요구사항 정의서(PRD)를 먼저 확정한 뒤 코드를 요청하여 생성-수정 반복을 최소화
- **단위 분할 요청**: 한 번에 하나의 산출물(문서 또는 기능) 단위로 요청
- **골격 1회 생성**: 코드 구조와 함수 시그니처만 우선 생성하고, 구현은 함수 단위로 분할
- **합성 데이터**: 실데이터 대신 구조만 모사한 합성 데이터 사용 (보안·재현성)

---

## 1단계 — 설계·골격

### P1. 주제 구체화

```
(DLC 코팅 장비 이상탐지·조기경보 연구 기획 논의 내용 공유 후)
이 주제로 프로토타입을 설계해줘. 범위는 규칙 기반 baseline까지로 한정.
```

→ 실제 현장 문제(8시간+ batch의 사후 불량 발견, NG 1~5%)를 분석 가능한 범위로 축소. 학습 모델·edge 배포·inter-batch 분석은 후속 단계로 분리하고, 1단계는 "multi-phase 로그 → phase 구분 → 규칙 기반(임계값/이동평균±kσ) 탐지 → 시각화"로 확정. 실데이터는 보안상 미사용, 구조(폐루프 제어 변수는 안정·자유응답 변수만 drift)만 모사한 합성 데이터 생성기를 포함하기로 결정.

### P2. 요구사항 정의서(PRD) 작성

```
문제의 본질, 해결의 필요성, 기존 방법의 한계, 제안 방법과 차별점,
구현 계획(AI 도구 기반), 검증 계획 흐름으로 DLC 코팅 공정 센서 이상
탐지 프로토타입의 PRD를 1페이지로 작성해줘. 제외 범위(실시간 연동,
학습 모델, edge 배포)를 명시하고 입력/출력 데이터 형식을 구체적으로.
```

→ PRD.md 작성. 핵심 결정: 입력 스키마 `timestamp, phase, sensor_id, value`, 핵심 기능 3개 = 파싱·phase 분리 / 규칙 기반 탐지 / 시각화·리포트.

### P3. 코드 골격 1회 생성

```
위 PRD 전문을 기반으로 프로젝트 구조와 핵심 함수의 골격만 생성해줘.
구조와 함수 시그니처만, 구현은 비워둬 (raise NotImplementedError).
폴더 구조: src/(load, generate_data, detect, visualize, main), tests/,
data/. pandas/matplotlib만 사용, 로컬 CLI 실행.
```

→ 모듈 6개 + 테스트 골격 생성. `python -m py_compile` 통과 확인.

### P4. 문서화

```
기술보고서 구조(초록-서론-방법-구현-결과-고찰-결론, 사용 매뉴얼 부록)에
맞춰 보고서 초안을 작성해줘. 설계 단계이므로 서론·방법은 채우고
구현·결과는 2단계에서 채울 틀만.
```

→ REPORT.md 초안 작성.

### P5. PRD 재구성 — 개조식·설득력 보강

```
PRD의 배경·문제 정의·필요성·기존 한계·제안 부분을 도메인 비전문가도
이해할 수 있도록 부연을 더해 개조식(사업계획서 형식)으로 재구성해줘.
핵심은 간단명료하게, 해결의 필요성이 설득되도록.
```

→ PRD 1~4절을 개조식으로 전환, DLC 공정 배경 부연 추가. 보고서 2.2절에 변수군 비교 분석 표 추가.

### P6. PRD 논리 구조 강화 — 심사 관점 재구성

```
문제 정의와 접근 방법이 다양한 분야의 심사위원을 설득할 수 있도록
기승전결 구조로 재구성해줘: 배경(기) → 손실 구조·핵심 질문(승) →
기존 방법의 한계·핵심 통찰(전) → 진단에 1:1 대응하는 설계(결).
```

→ 배경→문제→원인 진단→접근의 인과 사슬로 재배치. "진단된 한계 ↔ 설계 대응" 표와 단계적 로드맵(누적 검증 구조) 추가.

---

## 2단계 — 구현·검증

### P7. 합성 데이터 생성기 구현

```
generate_data.py를 구현해줘. PHASE_PLAN 5단계(1시간 축소 모형, 1Hz),
제어 변수는 CV 0.1% 미만으로 설정값 추종, 자유응답 변수는 곡선·drift·잡음.
이상 3종(펌핑 누설로 진공 플로어 0.4→4 Pa, main_dlc 압력 스파이크 3회,
온도 drift 가속 +6.5°C)을 주입하고 검증용 ground-truth(labels.json) 저장.
```

→ 시드 고정(42). 실제 8시간 공정의 시간 비율을 유지한 1/8 축소 모형.

### P8. 탐지·시각화·CLI 구현 (기능 단위 분할)

```
detect.py: 고정 임계값 {(phase, sensor): (low, high, skip_first_s)} 규칙과
phase별 독립 rolling ±k·σ 규칙(자유응답 변수만 대상) 구현.
visualize.py: phase 경계 점선·이상점 빨간 마커·압력은 로그축, 축 라벨·단위.
main.py: load → detect → 이상치 CSV·차트 파이프라인.
tests: 임계값 정확성·phase 경계 불침범·재현성 3개 테스트.
```

→ 기본 파라미터 window=120s, k=4.0 (k=3은 정상 잡음에서 오탐 발생 → 4.0으로 조정). 테스트 3종 통과.

### P9. 검증 중 단건 수정 기록 (전체 재생성 0회)

- **수정 1**: pandas FutureWarning(빈 DataFrame concat) → `run_all_rules`에서 빈 결과를 제외하고 concat하도록 해당 함수만 수정
- **수정 2**: temp_drift의 ground-truth 시작점을 근사치(phase의 55% 지점)로 정의해 정당한 검출 84건이 오탐으로 집계됨 → "냉각 이상은 phase 시작부터 존재하는 결함"이므로 라벨을 phase 전 구간으로 정정 (도메인 판단에 따른 수정 — 도구가 아닌 사람의 역할)

→ **최종 검증 결과**: 4종 batch에서 주입 이상 5/5 구간 검출, 오탐 0건, 첫 경보는 batch 종료 대비 19~53분 조기(축소 모형 기준). 상세는 REPORT.md 4절.

## AI 도구 사용 명시

요구사항 정의서·코드 골격·문서 초안은 Claude로 생성하였다. 주제 선정, 범위 결정, 현장 문제 정의, 사실 검증은 저자가 수행하였다. 생성 코드와 직접 수정 부분은 commit 이력으로 구분한다.
