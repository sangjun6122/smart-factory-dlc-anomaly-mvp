# PROMPTS.md — 프롬프트 로그

> AI Coding Tools 활용 프로그래밍 · 14주차 과제
> 작성자: JAVIS · 도구: Claude (Cowork) + Claude Code
> 본 파일은 구현 전체 과정에서 사용한 **결정적 프롬프트**를 기록한다 (수업 가이드: 전체가 아닌 핵심 5~10개 발췌). 15주차 구현 과정의 프롬프트는 하단에 계속 추가한다.

## 작업 방식 (토큰 절감 전략 적용)

- 명세(PRD)를 먼저 확정한 뒤 코드를 요청 — 왕복 횟수 최소화
- 한 번에 하나의 산출물 단위로 요청 (작은 단위)
- 뼈대 생성은 1회만: "구조와 함수 시그니처만, 구현은 비워둬"
- 실데이터 대신 구조만 모사한 합성 데이터 사용 (보안·재현성)

---

## P1. 과제 접근 방향 파악

```
* 다음 수업(15주) 시작 전까지 PRD 1페이지 + 프로토타입 뼈대 코드 제출
* 클로드 코드를 통해 프롬프트 생성 → MVP 생성
오늘까지 해야되는 과제인데 프로젝트 폴더 내 레퍼런스를 통해 어떻게 접근하면 되는지 확인해줘
```

→ Week14 강의자료(PPT)에서 과제 요구사항(PRD 템플릿·뼈대 생성 워크플로·평가 루브릭)과 예시 사업계획서 12건의 활용법(AS-IS 현장 문제 근거)을 도출.

## P2. 주제 선정

```
프로토타입 주제를 어떤 것으로 할까요? → 센서 이상치 탐지 선택
```

→ 이후 실제 업무에서 겪는 문제(DLC 코팅 batch의 사후 불량 발견)로 주제를 구체화하기로 결정.

## P3. 주제를 실제 현장 문제로 전환

```
(DLC 코팅 장비 예지보전/이상탐지 연구 기획 대화 내용 공유 후)
이 주제를 가지고 과제물을 만드는 건 어떠니?
```

→ 과제 범위에 맞게 대폭 축소하는 조건으로 채택: 딥러닝·edge 배포·inter-batch 분석은 향후 과제로 빼고, MVP는 "multi-phase 공정 로그 → phase 구분 → 룰 기반(임계값/이동평균±kσ) 탐지 → 차트"로 한정. 실데이터는 보안상 미사용, 구조(제어 변수는 안정·자유응답 변수만 drift)만 모사한 합성 데이터 생성기 포함.

## P4. PRD 1페이지 작성

```
슬라이드 13 PRD 템플릿(문제/배경, 목표/범위, 사용자/시나리오, 입력/출력,
핵심 기능 MVP 3개, 비기능/제약)에 맞춰 DLC 코팅 공정 센서 이상 탐지
프로토타입의 PRD를 1페이지로 작성해줘. 하지 않을 것(실시간 연동, 딥러닝,
edge 배포)을 명시하고, 입력/출력 데이터 형식을 구체적으로.
```

→ PRD.md 생성. 핵심 결정: 입력 스키마 `timestamp, phase, sensor_id, value`, MVP 3기능 = 파싱·phase 분리 / 룰 기반 탐지 / 시각화·리포트.

## P5. 프로토타입 뼈대 1회 생성 (실습 3)

```
위 PRD 전문을 기반으로 프로젝트 구조와 핵심 함수의 뼈대만 생성해줘.
구조와 함수 시그니처만, 구현은 비워둬 (raise NotImplementedError).
폴더 구조: src/(load, generate_data, detect, visualize, main), tests/,
data/. pandas/matplotlib만 사용, 로컬 CLI 실행.
```

→ 뼈대 코드 6개 파일 + 테스트 뼈대 생성. `python -m py_compile`로 문법 검증 통과 확인.

## P6. 문서화 (보고서 초안·README)

```
기술보고서 구조(IMRaD 변형: 초록-서론-방법-구현-결과-고찰-결론)에 맞춰
보고서 초안을 만들어줘. 14주차 시점이므로 서론·방법은 채우고
구현·결과는 15주차에 채울 틀만. 사용 매뉴얼 섹션 포함.
```

→ REPORT.md 초안 생성.

---

## 15주차 구현 로그 (이어서 기록 예정)

- [ ] P7. generate_data 구현 요청
- [ ] P8. load/detect 구현 요청 (기능 1개씩)
- [ ] P9. visualize/main 구현 요청
- [ ] P10. 실패한 기능 단건 재요청 기록 (있을 경우)

## AI 사용 명시

PRD·뼈대 코드·문서 초안은 Claude(Cowork)로 생성했으며, 주제 선정·범위 결정·현장 문제 정의·사실 검증은 본인이 수행했다. 생성 코드와 직접 수정 부분은 commit 이력으로 구분한다.
