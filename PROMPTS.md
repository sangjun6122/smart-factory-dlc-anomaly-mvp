# PROMPTS.md — 바이브 코딩 실행 전략 & 프롬프트 로그

> 작성자: 송상준 · 2026-06-12
> **수행 원칙: 모든 코드는 AI 코딩 도구가 생성한다 — 사람은 명세·리뷰·판정만 담당한다 (직접 코딩 0줄).**
> 본 문서는 두 부분으로 구성된다. **PART A: 실행 전략(플레이북)** — 도구 구성, 에이전트 정의(시스템 지침), 구현 계획부터 검증(R&V)까지의 전략적 단계별 프롬프트. **PART B: 실행 로그** — 실제 수행 기록과 판정. 원칙: PRD가 뼈대, 마일스톤이 단계, 프롬프트는 마일스톤에서 도출된 검증 가능한 구체 명세다. 그리고 프롬프트는 3층 설계(프롬프트 × 컨텍스트 × 하네스)의 한 층일 뿐이다 — 최소한의 수정으로 최선의 출력을 얻는 구조는 세 층이 함께 만든다.

---

# PART A. 실행 전략 (플레이북)

## A-1. 3층 설계 — 프롬프트 × 컨텍스트 × 하네스

| 층 | 답하는 질문 | 본 프로젝트의 적용 |
|---|---|---|
| **프롬프트 엔지니어링** | 무엇을 어떻게 요청하는가 | 5요소 템플릿([역할][맥락][작업][제약][완료 기준]), 완료 기준의 테스트 번역(T1~T3), 역할별 출력 형식 분리 |
| **컨텍스트 엔지니어링** | 모델이 보는 정보를 어떻게 설계하는가 | 프로젝트 메모리 파일 상주(`CLAUDE.md`·`AGENTS.md`), 발췌 규칙, 세션 경계, SSOT 지정 |
| **하네스 엔지니어링** | 요청 밖의 실행·검증 루프를 어떻게 짜는가 | 게이트 체인, 자기 검증 의무, 단건 수정 프로토콜, stop 조건, 도구 간 교차 검증 |

**□ 컨텍스트 엔지니어링 세부**

- **상주 메모리**: 설계 원칙·명령어·게이트·작업 규칙을 `CLAUDE.md`(Claude Code용)와 `AGENTS.md`(Codex용)에 저장해 모든 세션에 자동 주입 — 매 요청의 맥락 재설명 제거(토큰 절약), 응답 일관성 확보. **두 파일은 본 저장소에 포함되어 있다.**
- **SSOT 지정**: "명세의 유일한 진실 원천은 PRD.md, 충돌 시 PRD 우선"을 메모리에 명시 — 도구가 임의 가정으로 명세를 재해석하는 것을 차단
- **발췌 규칙**: 요청에는 전체 파일이 아니라 명세 해당 절 + 대상 함수만. 에러는 스택트레이스 마지막 줄 + 최소 재현 입력만
- **세션 경계**: 마일스톤마다 새 세션 시작(누적 맥락의 비용·오염 차단). 이월이 필요한 상태는 문서(PRD·PROMPTS)와 메모리 파일로만 전달

**□ 하네스 엔지니어링 세부**

- **게이트 체인**: `py_compile → pytest 3종 → 4종 batch 무오류 실행 → ground-truth 집계(G1~G4)` — 앞 게이트를 통과하지 못하면 다음 단계 진행 금지
- **자기 검증 의무**: Builder는 구현 직후 스스로 테스트를 실행하고 결과를 보고 — 사람의 역할은 '실행'이 아니라 '판정'으로 축소
- **단건 수정 프로토콜**: 실패 시 증거 패키지(함수명·재현 입력·기대/실제)만 Improver에 전달 — 전체 코드 접근 차단으로 수정 범위 폭주 방지
- **stop 조건**: 동일 실패 2회면 자동 재시도 중단, 사람에게 에스컬레이션 — 무한 재생성(토큰 폭증) 방지
- **교차 검증**: 작성 도구 ≠ 검증 도구 (Claude Code 작성 → Codex 검토)

## A-2. 도구 구성과 교차 검증 전략

| 도구 | 담당 | 선정 이유 |
|---|---|---|
| **Claude Code** | 주 구현 — 계획·구현·테스트 실행 | 멀티파일 작업, 에이전트(역할) 정의, 로컬 테스트 실행 |
| **Codex** | 독립 크로스체크 — 코드·계획 교차 검토 | **작성 도구와 다른 도구**로 검토해 작성자 편향(self-review bias) 제거 |

핵심 원칙: **코드를 생성한 도구가 자기 코드의 최종 검증자가 되지 않는다.** 같은 명세를 두 도구가 다른 역할로 다루게 하여, 한 도구의 가정 오류가 그대로 통과되는 것을 막는다.

> **적용 범위 명확화**: 본 1단계는 Claude(Cowork/Claude Code) 중심으로 구현하였다. Codex 교차 검증 프로세스(A3 지침, S2·S6 프롬프트)는 본 문서에 설계되어 있으며, **제출 전 점검과 후속 단계(학습 모델)부터 적용할 예정이다.**

## A-3. 에이전트 정의 — 작업 전 선행 구축

아래 지침은 선언으로 끝나지 않고 **저장소 파일로 상주**한다: A1·A2·A4·A5는 `CLAUDE.md`(작업 규칙·게이트), A3은 `AGENTS.md`(점검 축·출력 형식). 단계 프롬프트는 이 위에서 '이번 작업'만 전달하면 된다 — 이것이 최소 수정으로 최선 출력을 얻는 구조다.

각 역할에 시스템 지침을 부여한 에이전트를 먼저 만들고, 이후 모든 단계는 해당 에이전트에 단계 프롬프트만 투입한다. (지침이 고정되므로 매 요청마다 맥락을 재설명할 필요가 없어 토큰이 절약되고 응답 일관성이 생긴다.)

### A1. Architect (Claude Code) — 큰 계획

```
[시스템 지침]
너는 이 프로젝트의 아키텍트다.
- 입력: PRD 전문. 모든 결정은 PRD §3(설계 원칙: phase별 독립 계산,
  자유응답 변수 중심)과 §4(정량 목표 G1~G4)를 근거로 인용할 것
- 책임: 마일스톤 분해, 모듈 경계, 입출력 계약(시그니처·타입·예외) 정의
- 출력: 마일스톤 표(산출물·검증 가능한 완료 기준), 인터페이스 명세
- 금지: 구현 코드 작성, 명세에 없는 기능 제안
```

### A2. Builder (Claude Code) — 작은 계획 + 구현

```
[시스템 지침]
너는 구현 담당 시니어 파이썬 엔지니어다.
- 입력: 마일스톤 1개 + 해당 인터페이스 명세 + 도메인 수치 명세
- 절차: ① 함수 단위 작업 순서(작은 계획)를 5줄 이내로 먼저 제시
        ② 승인 후 구현 ③ 완료 기준 self-check 결과를 한 줄 보고
- 제약: 시그니처 변경 금지, 명세 밖 기능 추가 금지, pandas/matplotlib
  외 의존성 금지, 코드만 출력(설명 최소화)
```

### A3. Cross-Reviewer (Codex) — 교차 검토

```
[시스템 지침]
너는 이 코드를 작성하지 않은 독립 리뷰어다.
- 입력: 산출 코드 + 원 명세(완료 기준 포함)
- 점검 축: ① 명세 위반 ② 경계 조건(빈 입력·std=0·윈도 미만 길이·
  phase/센서 결손) ③ 숨은 가정 ④ 수치 정확성(임계값·윈도·시드)
- 출력: 결함 목록만 — 각 결함에 심각도(상/중/하)와 재현 입력 첨부.
  칭찬·요약·재작성 금지
```

### A4. Verifier (Claude Code) — 테스트·판정

```
[시스템 지침]
너는 검증 담당이다.
- 책임: ① 완료 기준을 pytest로 번역 ② ground-truth(labels.json) 대조
  집계 ③ PRD §4의 G1~G4 판정
- 원칙: 판정 근거는 수치만. 실패 시 최소 재현 케이스를 만들어
  Improver에 전달할 증거 패키지(함수명+입력+기대/실제)를 작성
```

### A5. Improver (Claude Code) — 단건 수정

```
[시스템 지침]
너는 버그 수정 담당이다.
- 입력: Verifier의 증거 패키지(실패 함수 + 재현 입력 + 기대/실제)만.
  전체 코드베이스 접근 금지
- 책임: 최소 수정. 수정 범위 밖 코드 변경 금지, 수정 후 해당
  테스트 재실행 결과 보고
```

## A-4. 단계별 실행 프롬프트 — 구현 계획부터 R&V까지

```
S1 큰 계획(Architect) → S2 계획 크로스체크(Codex) → S3 개선 반영
→ [마일스톤 루프 M1~M4: S4 작은 계획 → S5 구현(Builder)
   → S6 코드 크로스체크(Codex) → S7 테스트(Verifier)]
→ S8 통합 검증·판정(Verifier) → S9 단건 개선(Improver) → S10 문서화
```

### S1 (Architect) 큰 계획 수립

```
PRD 전문 첨부. M1(골격)~M5(검증) 마일스톤으로 분해하라.
각 마일스톤에 산출물과 '검증 가능한' 완료 기준을 정의하고,
M3(탐지 엔진)의 완료 기준은 pytest로 번역 가능한 형태(T1~T3)로 쓸 것.
```

### S2 (Codex) 계획 크로스체크

```
PRD와 Architect의 마일스톤 계획 첨부. 다음을 점검하라:
① 계획이 PRD 정량 목표 G1~G4를 모두 커버하는가 (누락 매핑 표시)
② 완료 기준 중 측정 불가능한 것 ③ 마일스톤 간 의존성 오류.
결함 목록만 출력.
```

### S4 (Builder) 작은 계획 — 마일스톤 착수 직전

```
M3 명세 첨부. 구현 착수 전, 함수 단위 작업 순서와 각 함수의
리스크(까다로운 경계 조건) 1개씩을 5줄 이내로 제시하라. 코드는 아직 금지.
```

### S5 (Builder) 구현 — 예시: M3 탐지 엔진 (수치 명세 전문)

```
[맥락] PRD §3 진단↔설계 표 + M1 인터페이스. 입력 df 컬럼:
timestamp(datetime64)·phase(범주 5종)·sensor_id(str)·value(float)
[작업] detect.py 구현:
① detect_threshold — abs_limits={(phase,sensor):(low,high,skip_first_s)}.
  phase 시작 후 skip_first_s 경과분만 판정. 기본 사양:
  ("pumping","pressure_p1")=(None,2.0,420) /
  ("main_dlc","pressure_p1")=(0.2,0.7,0) /
  ("main_dlc","temp_chamber")=(None,37.0,0)
② detect_rolling_sigma — groupby(["phase","sensor_id"])로 경계 침범을
  구조적으로 차단 → rolling(window,min_periods=window) mean·std(하한 1e-9)
  → |value-mean| > k*std. 대상은 config.target_sensors(자유응답 2종)만
③ run_all_rules — 합집합, (timestamp,sensor_id,rule) 중복 제거
[완료 기준 = 테스트]
T1: pumping 5.0 Pa 600초 → skip(420s) 이후 정확히 180건 검출
T2: phase A(1.0±0.01)→B(100±0.01) 점프 → ±kσ 결과 empty (경계 불침범)
T3: 동일 시드 2회 실행 → assert_frame_equal 통과 (재현성)
```

(M2 데이터 생성기·M4 시각화도 동일 형식 — phase 길이 600/600/600/1500/300s,
압력 프로파일 100·exp(-t/70)+0.4, 이상 3종 수치, 차트 요구사항을 수치로 명세.
전문은 PART B 실행 로그 참조)

### S6 (Codex) 코드 크로스체크

```
detect.py와 S5 명세 첨부. A3 지침의 4개 축으로 점검하되, 특히
'std=0인 제어 변수가 target_sensors에 들어오는 경우'와 '윈도보다 짧은
phase'에서의 동작을 재현 입력과 함께 검증하라.
```

### S8 (Verifier) 통합 검증·판정

```
results/ 4종 실행 결과와 data/labels.json 첨부. 시나리오별
① 주입 구간 검출 수/전체 ② 구간 외 오탐 ③ 첫 경보의 batch 종료 대비
조기분을 집계하고 G1~G4를 판정하라. 출력: verification_summary.csv + 판정표.
```

### S9 (Improver) 단건 개선 — 증거 패키지 템플릿

```
[실패 함수] run_all_rules
[재현 입력] 이상치 0건인 정상 batch
[기대] 경고 없이 빈 결과 반환 / [실제] pandas FutureWarning 발생
이 함수만 최소 수정하라. 다른 코드 변경 금지.
```

---

# PART B. 실행 로그 (실제 수행 기록)

## B-0. 실제 프롬프트 로그 — 입력 → 응답 → 판단 (발췌 10건)

> 형식: **입력**(실제 사용한 프롬프트) → **응답**(AI 산출 요약) → **판단·조치**(사람의 결정). 상세 전략·명세 전문은 PART A 참조.

**#1 주제 구조화**
- 입력: `DLC 코팅 연구 기획 논의 요약 첨부. 1단계 프로토타입 범위로 구조화하라. 규칙 기반 baseline까지만, 학습 모델·edge는 후속 분리.`
- 응답: "multi-phase 로그 → phase 분리 → 규칙 탐지 → 시각화" 범위안 + 제외 목록
- 판단: 채택. 실데이터 금지·합성 데이터 사용은 사람이 추가 결정

**#2 PRD 초안 작성**
- 입력: `문제의 본질/필요성/기존 한계/제안/구현·검증 계획 구조로 PRD 1페이지 작성. 입출력은 스키마 수준으로.`
- 응답: PRD v1 (서술형) — 입력 스키마 `timestamp, phase, sensor_id, value` 제안
- 판단: 골자 채택, 문체·구조는 리뷰 대상으로 보류

**#3 PRD 비판 리뷰 (심사위원 관점)**
- 입력: `국책과제 심사위원 관점에서 약점만 지적하라 — 비전문가 이해도, 문제의 본질, 목표의 측정 가능성, 작업 범위, 논리 비약.`
- 응답: 결함 4건 (서술형 가독성 / 한계↔제안 연결 / 목표 측정 불가 / 범위 불명확)
- 판단: 4건 전부 수용 → 개조식·기승전결·G1~G4·In/Out 표로 2회에 걸쳐 개선

**#4 코드 골격 1회 생성**
- 입력: `PRD 전문 기반으로 src/(load, generate_data, detect, visualize, main)+tests/ 구조와 함수 시그니처·docstring만 생성. 구현은 raise NotImplementedError.`
- 응답: 6모듈 골격 + 테스트 3개 이름
- 판단: `py_compile` 통과 확인 후 채택. 리뷰에서 `target_sensors` 파라미터 보강 1건

**#5 합성 데이터 생성기 구현**
- 입력: `generate_data.py 구현 — phase 600/600/600/1500/300s(1Hz), 펌핑 100·exp(-t/70)+0.4 Pa, 제어 3종 CV<0.1%, 이상 3종(누설 플로어 4 Pa/스파이크 +0.18 Pa×3/온도 drift +6.5°C) 주입, labels.json 저장, 시드 42.`
- 응답: generate_data.py + data/ 4종 + labels.json
- 판단: 정상 batch 5센서 차트 육안 검증 후 채택. 이상 수치의 물리적 타당성은 사람이 지정

**#6 탐지 엔진 구현**
- 입력: `detect.py 구현 — abs_limits={(phase,sensor):(low,high,skip_first_s)}, phase별 groupby rolling ±k·σ(자유응답만, std 하한 1e-9). 완료 기준: T1(180건 정확 검출)/T2(phase 경계 불침범)/T3(재현성).`
- 응답: detect.py 구현 + 테스트 통과
- 판단: pytest 3/3 통과로 채택

**#7 시각화·CLI 구현**
- 입력: `visualize.py(phase 경계 점선·이상점 마커·압력 로그축·단위 라벨) + main.py(load→detect→CSV·PNG) 구현. Agg 백엔드, 코드만.`
- 응답: 구현 + 4종 batch 무오류 실행
- 판단: 차트 육안 검증(경계·마커·축) 후 채택

**#8 단건 수정 — FutureWarning**
- 입력: `[실패 함수] run_all_rules [재현] 이상치 0건 정상 batch [기대] 경고 없음 [실제] FutureWarning. 이 함수만 최소 수정.`
- 응답: 빈 결과 제외 후 concat으로 수정
- 판단: 경고 제거·테스트 재통과 확인

**#9 ground-truth 정정 (사람 주도)**
- 입력(사람의 지적): temp_drift 검출 84건이 오탐으로 집계됨 — 라벨 시작점이 근사치(55%)
- 조치: "냉각 이상은 phase 시작부터 존재하는 결함" → 라벨을 phase 전 구간으로 정정
- 판단: 도구가 아닌 도메인 지식의 역할을 보여준 사례

**#10 평가 방법론 교정 (사람 지적 → 도구 구현)**
- 입력(사람의 지적): `정상 데이터가 너무 작은 것 아닌가?` → 점검 결과 k=4가 평가 데이터로 튜닝된 누수 확인
- 입력(도구): `evaluate.py 구현 — 칼리브레이션(정상 20, 시드 42~61)에서 오탐 0 최소 k 산정, 시드 분리 시험 세트(정상 20+이상 3종×5시드)에서 FAR·검출률 보고.`
- 응답: k*=4.5 산정, 시험 세트 정상 오탐 0(0/20 batch)·이상 25/25 검출
- 판단: 기본값 k=4.5 갱신, 보고서 §2.3·고찰에 교정 과정 명시

## B-0-1. 사람이 직접 판단한 핵심 결정 (요약)

AI는 생성을 빠르게 했지만, 아래 결정은 모두 도메인 지식과 사람의 검토에서 나왔다.

| 결정 | 내용 | 비고 |
|---|---|---|
| 문제 선정 | 실제 현장 이슈(8시간+ batch 사후 불량)를 주제로 | 본인 업무 경험 |
| 정보 비대칭 통찰 | 제어 변수는 정보 없음, 자유응답 변수에 이상 집중 | 실로그 분석 경험 → 설계 전체의 토대 |
| 데이터 정책 | 실데이터 금지, 구조만 모사한 합성 데이터 | 보안·윤리 |
| 이상 시나리오 설계 | 3종의 물리적 타당성 + "어느 규칙에 잡혀야 하는가" 검증 의도 내장 | 공정 지식 |
| ground-truth 정의 | "냉각 이상은 phase 시작부터 존재하는 결함" — 라벨 정정 | 도구 산출 오류를 도메인 판단으로 교정 |
| 평가 방법론 교정 | "평가 데이터로 k를 튜닝하고 있다"는 누수 발견 → 칼리브레이션/시험 분리 | 무엇이 공정한 시험인지의 판단 |
| 파라미터 확정 | 칼리브레이션 결과를 보고 k=4.5 채택 | 판정은 사람 |


> 본 1단계는 Claude(Cowork/Claude Code)에서 A1~A5 역할을 독립 세션·역할 프롬프트로 분리해 수행했다. Codex 교차 검증(S2·S6)은 동일 프롬프트로 제출 전 최종 점검과 2단계(학습 모델)부터 정식 적용한다 — 역할·프롬프트가 위에 고정되어 있으므로 도구 교체 비용은 없다.

## M0. 설계 — PRD (플랜→리뷰→개선 2회)

- **P1 (Architect)** 문제 구조화: "multi-phase 로그 → phase 분리 → 규칙(임계값/±kσ) 탐지 → 시각화" 확정. *사람의 판단*: 실데이터 보안상 미사용 → 정보 비대칭 구조만 모사한 합성 데이터.
- **P2~P4 (플랜→리뷰→개선)**: 심사위원 관점 리뷰가 지적한 4개 결함을 PRD에 반영 —

| 리뷰 지적 | 개선 반영 |
|---|---|
| 서술형·비전문가 이해 불가 | 개조식 전환 + 공정 배경 부연 (§1) |
| 한계↔제안 논리 연결 약함 | 기승전결 재배치 + 진단↔설계 1:1 표 (§3) |
| 목표가 측정 불가 | 정량 목표 G1~G4 신설 (§4) |
| 작업 범위 불명확 | In/Out 범위 표 + 마일스톤 M1~M5 (§4·§5) |

## M1. 골격 — S4·S5 수행

- 모듈 6개 시그니처·docstring(입출력 계약·예외) 생성, `py_compile` 통과.
- 인터페이스 리뷰: DetectConfig에 `target_sensors`·`abs_limits`를 두어 설계 원칙 2개(정보 비대칭·phase 독립)를 **파라미터로 강제**하기로 확정.

## M2. 합성 데이터 — S5 수행 (수치 명세 전문은 A-3 형식)

- phase 5단계(600/600/600/1500/300s, 1Hz), 제어 3종 CV<0.1%, 자유응답 2종 drift, 이상 3종 주입 + labels.json. 시드 42.
- *사람의 판단*: 이상 설계에 **검증 의도**를 내장 — 압력 스파이크(+0.18 Pa)는 운전 상한(0.7 Pa) 미만이라 임계값 규칙이 침묵하고 ±kσ만 잡아야 하며(규칙 분담 검증), 온도 drift는 ±kσ가 추종하는 한계를 드러내고 사양 규칙(37°C)이 잡아야 함(한계 실증).

## M3~M4. 탐지·시각화 — S4~S7 수행

- S5 명세(상기 전문)로 1회 구현 통과. 경계 조건 리뷰: std 하한 1e-9·윈도 미만 skip 확인.
- *파라미터 튜닝(사람)*: k=3은 정상 잡음에서 이론상 ~0.3%/점 오탐 → 실행 확인 후 **k=4.0 확정** (G2 달성 조건).
- pytest T1~T3 통과. 4종 batch 파이프라인 무오류 실행.

## M5. 검증 — S8·S9 수행

단건 수정 2회 (전체 재생성 0회):

| # | 증상 | 원인 | 수정 범위 |
|---|---|---|---|
| 1 | pandas FutureWarning | 빈 DataFrame concat | `run_all_rules` 함수만 |
| 2 | temp_drift 오탐 84건 집계 | ground-truth 시작점을 phase 55% 지점으로 근사 — 정당한 검출이 오탐 처리 | 라벨 정의만 — "냉각 이상은 phase 시작부터 존재하는 결함"으로 정정. **도구가 아닌 도메인 판단** |

### P10 (Verifier) 평가 방법론 교정 — 칼리브레이션/시험 분리

사람의 지적("정상 데이터가 너무 작은 것 아닌가")에서 출발한 교정. 점검 결과 두 결함 확인: ① k=4를 평가에 쓴 동일 batch로 튜닝(데이터 누수) ② 정상 1개 batch 기준 오탐 평가의 통계적 빈약함 — 실제로 정상 20 batch로 늘리자 k=4에서 오탐 6건 발현.

```
[역할] Verifier
[작업] evaluate.py 구현 — ① 칼리브레이션 세트(정상 20, 시드 42~61)에서
오탐 0 최소 k 산정(격자 3.0~5.0) ② 시드 분리된 시험 세트(정상 20,
시드 100~119 + 이상 3종×5시드, 200~204)에서 FAR·구간 검출률 평가
[제약] 파라미터 산정 데이터를 성능 보고에 재사용 금지
[완료 기준] calibration.csv + evaluation_summary.csv
```

→ k*=4.5 산정(기본값 갱신). 시험 세트: 정상 20 batch 오탐 0(발생 batch 0/20), 이상 25/25 구간 검출, 구간 외 오탐 0.

**최종 판정** — G1 ✅ 독립 시험 세트 이상 25/25 구간 검출 · G2 ✅ 시험 세트 정상 20 batch 오탐 0건 · G3 ✅ 재현성 테스트 통과 · G4 ✅ 매뉴얼 기반 재실행 확인. 부가: 첫 경보 19~53분 조기(1/8 축소 모형). 상세 REPORT.md §4.

---

## 회고 — 무엇이 결과 품질을 만들었나

- **하네스가 반복을 구조로 흡수**: 게이트 체인과 자기 검증 의무 덕에 사람의 개입이 '재요청'이 아니라 '판정'으로 줄었다. 상주 메모리(CLAUDE.md/AGENTS.md)는 매 요청의 맥락 비용을 0에 수렴시켰다.
- **에이전트 선행 정의**: 역할 지침을 고정하니 단계 프롬프트가 짧아지고(맥락 재설명 불필요) 응답 일관성이 생겼다. "코드만"(Builder) / "결함만"(Reviewer)의 출력 분리가 토큰과 초점을 동시에 잡았다.
- **큰 계획/작은 계획 분리**: 마일스톤(큰 계획)과 착수 직전의 함수 단위 순서(작은 계획)를 분리하니, 방향 오류는 계획 단계에서, 구현 오류는 함수 단위에서 각각 싸게 잡혔다.
- **완료 기준의 테스트 번역**: "잘 동작"이 아닌 T1~T3 수준의 assert 가능한 기준을 프롬프트에 내장하자 막연한 재생성 없이 단건 수정 2회로 수렴했다.
- **교차 검증의 가치**: 자기 코드를 자기가 검증하지 않는다는 원칙(Codex 크로스체크 지정)은 작성자 편향을 구조적으로 차단한다.
- **도구와 사람의 경계**: 생성은 도구가 빨랐지만 — 정보 비대칭 통찰, 이상 시나리오의 물리적 타당성·검증 의도 설계, ground-truth 정의, 그리고 '평가에 쓴 데이터로 파라미터를 튜닝하고 있다'는 방법론 결함의 발견은 모두 사람의 판단이었다. 도구는 빠르게 만들지만, 무엇이 공정한 시험인지는 사람이 정한다.

## AI 도구 사용 명시

본 1단계는 Claude(Cowork/Claude Code) 중심으로 구현하였다. Codex 교차 검증 프로세스는 설계 완료 상태이며 제출 전 점검과 후속 단계에 적용할 예정이다. 주제 선정, 범위·목표 정의, 도메인 수치 명세, ground-truth 정의, 사실 검증은 저자가 수행하였다. 항목별 수행 주체는 REPORT.md 부록 B의 기여도 표 참조.
