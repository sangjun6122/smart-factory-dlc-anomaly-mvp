# 기술보고서 — DLC 코팅 공정 센서 이상치 탐지 MVP

> AI Coding Tools 활용 프로그래밍 · 최종 프로젝트 (14주차 초안, 15주차 완성 예정)
> 작성자: JAVIS (ssj.mlboot@gmail.com)
> ※ `[15주차]` 표시 부분은 구현 완료 후 채운다.

## 초록

DLC 코팅 한 batch는 8시간 이상 소요되며, 공정 중 미세 이상은 종료 후 외관·접착 검사에서야 불량(NG 1~5%)으로 확인되어 큰 손실을 만든다. 본 프로젝트는 그 첫 단계로, multi-phase 공정 센서 로그(CSV)를 phase별로 나눠 임계값·이동평균(±k·σ) 규칙으로 이상치를 탐지하고 차트·알람 목록을 출력하는 MVP를 구축했다. 실데이터 대신 공정 구조를 모사한 합성 데이터를 사용해 재현성을 확보했다. `[15주차: 결과 수치 한 문장 추가]`

## 1. 서론

### 1.1 문제 정의

PVD 기반 DLC 코팅 공정은 펌핑→전처리→중간층→본 증착→벤팅의 다단계로 8시간 이상 진행된다. 펌핑 단계의 진공 누설, 챔버·제품 오염 같은 이상의 전조는 단일 변수 임계치로는 보이지 않는 미세한 다변량 패턴으로 나타나며, 현재는 batch 종료 후 검사에서야 불량이 확인된다. 50채널 이상의 로그를 사람이 실시간 감시하는 것은 비현실적이다. 본 과제는 공정 로그를 phase 단위로 분석해 이상치를 자동 식별하는 가장 단순한 베이스라인(룰 기반)을 만든다.

### 1.2 목표와 범위

목표는 "CSV 입력 → phase 구분 → 룰 기반 이상 탐지 → 이상치 표 + 차트" 로컬 도구이다. 실시간 장비 연동, 딥러닝 모델, 이전 batch 이력 분석은 범위에서 제외하고 향후 과제로 남긴다. 이 룰 기반 도구는 향후 학습 기반 탐지 연구의 비교 베이스라인 역할도 한다.

## 2. 방법

### 2.1 설계

파이프라인은 입력(load) → 처리(detect) → 출력(visualize) 3모듈로 분리했다. 탐지 규칙은 두 가지다: ① 고정 임계값(공정 사양 기반 절대 한계), ② 이동평균 ±k·σ(같은 phase 안에서의 국소 거동 대비 편차). 롤링 계산이 phase 경계를 넘으면 단계 전환 자체를 이상으로 오인하므로 phase별 독립 계산을 설계 원칙으로 했다.

### 2.2 데이터 설계

실제 사내 데이터는 보안상 사용하지 않고, 실제 공정 로그의 구조적 특징만 모사한 합성 데이터를 생성한다. 핵심 모사 포인트는 변수의 두 부류 구분이다. 제어(closed-loop) 변수(전류·전압·가스유량)는 변동계수(CV) 0.1% 미만으로 안정한 반면, 자유응답(free-response) 변수(챔버 압력·온도)는 수 % 수준의 변동과 drift를 가진다. 따라서 이상 신호는 주로 자유응답 변수에 나타나도록 이상 시나리오 3종(펌핑 지연, 압력 스파이크, 온도 drift 가속)을 주입한다. 난수 시드를 고정해 같은 입력이면 같은 출력이 나오게 했다.

### 2.3 AI 도구 활용 전략

Claude(Cowork)를 사용해 PRD를 먼저 1쪽으로 확정한 뒤 뼈대 코드를 1회 생성하는 명세-우선 방식으로 토큰을 절감했다. 기능 구현은 15주차에 함수 단위로 쪼개 요청하고, 실패 시 해당 기능만 재요청한다. 핵심 프롬프트는 PROMPTS.md에 기록한다.

## 3. 구현

### 3.1 프로젝트 구조

```
dlc-anomaly-mvp/
├── PRD.md / PRD.pdf        # 요구사항 1쪽
├── REPORT.md               # 본 보고서
├── PROMPTS.md              # 프롬프트 로그
├── requirements.txt        # pandas, matplotlib, pytest
├── src/
│   ├── load.py             # CSV 파싱·phase 분리 (기능 1)
│   ├── detect.py           # 임계값·이동평균±kσ 탐지 (기능 2)
│   ├── visualize.py        # 차트·알람 CSV 출력 (기능 3)
│   ├── generate_data.py    # 합성 batch 생성기
│   └── main.py             # CLI 엔트리
├── tests/test_detect.py    # 단위 테스트
├── data/                   # 합성 샘플 CSV [15주차]
└── results/                # 차트·알람 출력 [15주차]
```

### 3.2 핵심 코드

`[15주차: detect_rolling_sigma 핵심 10~20줄 발췌 + 데이터 흐름 그림 1개]`

## 4. 결과

`[15주차: 합성 batch 4종(정상 1 + 이상 3)에 대한 탐지 결과 표 — 주입 이상 대비 검출률·오탐 건수, 센서별 차트 그림, 각각 한 줄 해석]`

| 시나리오 | 주입 이상 | 검출 여부 | 오탐 건수 |
|---|---|---|---|
| 정상 batch | — | — | `[ ]` |
| 펌핑 지연 | pumping phase 압력 하강 지연 | `[ ]` | `[ ]` |
| 압력 스파이크 | main_dlc phase P1 스파이크 | `[ ]` | `[ ]` |
| 온도 drift | 챔버 온도 drift 가속 | `[ ]` | `[ ]` |

## 5. 고찰

`[15주차: 잘 된 점 / 한계(룰 기반의 한계 — 미세 다변량 패턴 미검출 사례) / AI 도구가 도움이 된 지점과 막힌 지점 회고]`

미리 적어두는 예상 한계: 룰 기반은 단일 변수 편차만 잡으므로, 여러 변수가 정상 범위 안에서 조합으로만 드러나는 이상은 놓친다. 이것이 학습 기반 접근(향후 과제)의 필요 근거다.

## 6. 결론·향후 과제

본 MVP는 DLC 공정 로그의 phase-aware 룰 기반 이상 탐지 베이스라인을 제공한다. 향후 과제: ① 정상 데이터 기반(one-class) 학습 모델로 다변량 미세 패턴 탐지, ② 이전 batch 이력(inter-batch) 컨텍스트 반영, ③ edge 디바이스 실시간 추론·경고(latching) — 이는 진행 중인 연구(실시간 조기경보 시스템)로 연결된다.

## 부록 A. 사용 매뉴얼

```bash
# 1. 환경 설치 (Python 3.10+)
pip install -r requirements.txt

# 2. 합성 샘플 데이터 생성 [15주차 구현 후]
python -m src.generate_data

# 3. 이상치 탐지 실행
python -m src.main data/batch_ng_pressure_spike.csv --out results/
#   --window 60   롤링 윈도(초)
#   --k 3.0       k·σ 배수

# 4. 결과 확인
#   results/anomalies.csv      이상치 목록
#   results/<sensor_id>.png    센서별 차트 (phase 경계·이상점 표시)

# 5. 테스트
python -m pytest tests/
```

## 부록 B. AI 사용 명시 및 출처

PRD·뼈대 코드·문서 초안 작성에 Claude(Cowork, Claude Code)를 사용했다. 주제 선정, 현장 문제 정의, 공정 도메인 사실관계, 최종 검증은 본인이 수행했다. 프롬프트 로그는 PROMPTS.md 참조. 실제 사내 데이터·고객 정보는 포함하지 않았다.
