# PRD — DLC 코팅 공정 센서 이상 조기알림 프로토타입

| 과목 | AI Coding Tools 활용 프로그래밍 (14주차 과제) |
|---|---|
| 작성자 | JAVIS (ssj.mlboot@gmail.com) |
| 작성일 | 2026-06-12 |
| 도메인 | 스마트팩토리 — 센서·공정 데이터 (PVD/DLC 코팅) |

## 1. 문제 / 배경

DLC(Diamond-Like Carbon) 코팅 한 batch는 펌핑→전처리→중간층→본 증착→벤팅까지 8시간 이상 소요된다. 공정 중 발생한 미세 이상(진공 누설, 챔버·제품 오염 등)은 batch 종료 후 외관·접착 검사에서야 불량(NG, 전체의 1~5%)으로 확인되며, 장비 점유 시간 손실과 회복 불가능한 모재 손상으로 이어진다. 실제 코팅 현장(당사)에서 겪는 문제로, 50개 이상 센서 채널의 미세 drift와 변수 간 조합 패턴을 사람이 실시간으로 잡기 어렵다. 본 과제는 그 첫 단계로, 공정 로그를 phase별로 나눠 룰 기반 이상치를 탐지하는 MVP를 만든다.

## 2. 목표 / 범위

**목표**: multi-phase 공정 센서 로그 CSV를 입력받아 phase를 구분하고, 임계값·이동평균(±k·σ) 규칙으로 이상치를 탐지해 알람 목록과 차트를 출력하는 로컬 실행 도구.

**하지 않을 것**: 실시간 장비 연동(TCP/IP·edge 배포), 딥러닝 모델, 이전 batch 이력(inter-batch) 분석, 자동 정지 — 모두 향후 과제(졸업연구 연계)로 명시.

## 3. 사용자 / 시나리오

공정 운영자가 batch 종료 후(또는 진행 중 내려받은) 센서 로그 CSV를 도구에 입력 → phase별 이상치 표와 차트를 확인 → 점검이 필요한 단계·변수를 파악한다.

## 4. 입력 / 출력

- **입력**: `timestamp, phase, sensor_id, value` 형식 CSV (1Hz). 실데이터는 보안상 미사용 — 실제 공정 구조(제어 변수는 CV 0.1% 미만으로 안정, 압력·온도 등 자유응답 변수만 drift)를 모사한 **합성 데이터 생성기** 포함
- **출력**: ① 이상치 목록 CSV (`timestamp, phase, sensor_id, value, rule, threshold`) ② 센서별 시계열 차트 PNG (phase 경계 + 이상점 마킹)

## 5. 핵심 기능 (MVP, 3개)

1. **CSV 파싱·phase 분리**: 컬럼·타입 검증 후 phase별 DataFrame 분리 (입력)
2. **룰 기반 이상치 탐지**: phase별 고정 임계값 + 이동평균 ±k·σ 규칙, 파라미터 설정 가능 (처리)
3. **시각화·리포트**: 시계열 차트에 phase 경계·이상점 표시 + 이상치 CSV 저장 (출력)

부가: 합성 batch 데이터 생성기(정상 패턴 + 펌핑 지연·압력 drift 등 이상 주입).

## 6. 비기능 / 제약

- Python 3.10+, pandas / matplotlib만 사용, 로컬 CLI 실행 (`python -m src.main`)
- 외부 네트워크 전송 없음, 실제 사내 데이터 미사용(구조만 모사한 합성 데이터)
- 난수 시드 고정 — 같은 입력이면 같은 출력 (재현성)
