# 요구사항 정의서 (PRD) — DLC 코팅 공정 센서 데이터 기반 이상 탐지 프로토타입

| 프로젝트 | DLC 코팅 공정 이상 조기탐지 연구 — 1단계(설계·프로토타입) |
|---|---|
| 작성자 | 송상준 (ssj.mlboot@gmail.com) · 2026-06-12 |
| 분야 | 스마트팩토리 — 공정 센서 데이터 분석 (PVD/DLC 코팅) |
| 수행 방식 | AI 코딩 도구 기반 개발(vibe coding) — 명세(PRD) 우선 워크플로 |

## 1. 배경 및 문제 정의

DLC(Diamond-Like Carbon) 코팅은 고경도·저마찰 특성으로 자동차 부품, 사출금형, 정밀공구 등에 적용되는 표면처리 공정이다. 단일 batch는 펌핑→전처리→중간층→본 증착→벤팅의 다단계로 8시간 이상 소요되며, 공정 중 발생한 미세 이상(진공 누설, 챔버·제품 오염, 클리닝 불량 등)은 진행 중에는 드러나지 않고 batch 종료 후 외관·접착 검사에서 불량(전체의 1~5%)으로 확인된다.

문제의 본질은 두 가지다. 첫째, 이상의 전조는 **단일 변수의 한계 초과가 아니라 정상 범위 내의 미세한 drift와 다변량 결합 패턴**으로 나타나며, 동일 변수라도 공정 phase에 따라 의미가 달라 50채널 이상의 로그를 육안으로 감시하는 것은 불가능하다. 둘째, 채널 간 **정보 비대칭**이 존재한다 — 폐루프 제어 변수(전류·전압·유량)는 제어계가 변동을 흡수해 정보량이 거의 없고, 이상의 흔적은 자유응답 변수(챔버 압력·온도)에 집중된다.

## 2. 해결의 필요성

불량이 batch 종료 후에야 확인되는 현행 사후 확인 체계에서는 한 번의 불량이 8시간 이상의 장비 점유 손실, 재처리 비용, 회복 불가능한 모재 손상, 납기 지연으로 이어진다. NG 비율(1~5%)은 낮아 보이나 건당 손실이 커서, 공정 로그로부터 이상을 조기에 식별하는 능력은 코팅 서비스의 품질 일관성과 직결된다.

## 3. 기존 방법의 한계

단변량 임계치 기반의 통상적 감시(알람 설정·SPC 관리도)는 ① 정상 범위 안에서 진행되는 drift를 놓치고, ② phase 전환에 따른 정상적 거동 변화를 이상으로 오인하며, ③ 변수 간 결합으로만 나타나는 패턴을 다루지 못한다. 또한 전 채널을 동등하게 감시하므로 정보가 없는 제어 변수의 잡음에 신호가 희석된다.

## 4. 제안 방법과 차별점

공정 로그를 phase 단위로 분리한 뒤, phase별 독립 적용되는 두 규칙 — 공정 사양 기반 고정 임계값, 이동평균 ±k·σ — 으로 이상치를 탐지하는 **phase-aware 규칙 기반 도구**를 구현한다. 차별점은 도메인 구조를 설계에 직접 반영한 것이다: ① 이동 통계량이 phase 경계를 넘지 않도록 하여 단계 전환 오인을 제거하고(한계 ② 대응), ② 정보 비대칭에 근거해 자유응답 변수 중심으로 감시를 설계한다(한계 ③ 부분 대응). 다변량 결합 패턴 탐지는 후속 학습 기반 연구(one-class 모델, inter-batch 컨텍스트, edge 실시간 추론)의 대상이며, 본 프로토타입은 그 연구의 비교 기준(baseline)이 된다.

**핵심 기능 3개**: ① CSV 파싱·phase 분리(입력) ② 규칙 기반 이상치 탐지(처리) ③ phase 경계·이상점이 표시된 차트·이상치 목록 출력(출력)

## 5. 구현 계획 — 바이브 코딩(AI 코딩 도구 기반 개발)

LLM 기반 코딩 도구(Claude Code)를 활용한 명세 우선(spec-first) 방식으로 구현한다: ① 본 PRD를 확정하고 전문을 도구에 입력해 프로젝트 구조와 함수 시그니처(골격)를 1회 생성 → ② 기능을 함수 단위로 분할해 순차 구현 요청 → ③ 실패 시 해당 기능만 재요청(전체 재생성 금지) → ④ 전 과정의 주요 프롬프트와 의사결정을 PROMPTS.md에 기록해 추적성 확보. 이 방식은 생성-수정 반복과 개발 비용을 최소화하며, 본 프로젝트는 스마트팩토리 도메인 문제를 '문제 정의(PRD) → AI 도구 협업 구현 → 검증·기록'의 워크플로로 해결하는 실증 사례를 겸한다.

## 6. 검증 계획

실데이터는 보안상 사용하지 않고, 1절의 정보 비대칭 구조(제어 변수 CV 0.1% 미만, 자유응답 변수만 drift)를 모사한 합성 데이터로 검증한다. 정상 batch 1종과 이상 주입 batch 3종(펌핑 지연, 압력 스파이크, 온도 drift 가속)에 대해 **주입 이상의 검출 여부와 오탐 건수**를 평가한다. 난수 시드 고정으로 동일 입력→동일 출력의 재현성을 보장하고, 단위 테스트(pytest)로 규칙별 동작과 phase 경계 처리를 확인한다.

## 7. 명세 요약

| 항목 | 내용 |
|---|---|
| 사용자/시나리오 | 공정 운영자가 batch 센서 로그 CSV를 입력 → phase별 이상치 표·차트로 점검 대상 단계·변수 식별 |
| 입력 | `timestamp, phase, sensor_id, value` 형식 CSV (1Hz) + 합성 데이터 생성기 |
| 출력 | 이상치 목록 CSV(`…, rule, threshold`), 센서별 시계열 차트 PNG |
| 환경·제약 | Python 3.10+, pandas/matplotlib, 로컬 CLI. 외부 전송 없음, 실데이터·고객 정보 미포함 |
| 제외 범위 | 실시간 장비 연동(edge·TCP/IP), 학습 기반 모델, 이전 batch 이력 분석, 자동 정지 — 후속 연구 |
